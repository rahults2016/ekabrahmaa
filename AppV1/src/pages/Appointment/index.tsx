import Appointments from '@/features/Appointment/Appointment';

// This is just a passthrough component that re-exports the feature component
const AppointmentsPage = () => <Appointments />;

export default AppointmentsPage;